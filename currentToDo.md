Fix break and continue in loops
make system io with these sub includes: {
make filesystem module
make a module that has a way to get envs and exit program with codes
}
so basically you should be able to import std.system.filesystem or std.system.(insert name for envs and exit codes here)

make test.sh use the asm
string interpolation in asm
fix include in the codegen

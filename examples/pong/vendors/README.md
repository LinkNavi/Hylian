# Vendors

This directory contains vendor interface files used by the Pong example.

## sdl2

SDL2 bindings for Hylian, generated by `bindgen.py` from the SDL2 C headers.
Provides access to windowing, rendering, input, and event handling.

Requires `libSDL2-2.0.so.0` to be installed on the system at runtime.